{
  "name": "my-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "postinstall": "npm install --prefix server && npm install --prefix client",
    "db:migrate": "npm run prisma:migrate --prefix server",
    "db:generate": "npm run prisma:generate --prefix server",
    "build": "npm run prisma:generate --prefix server && npm run build --prefix client && npm run build --prefix server",
    "start": "npm run start:prod --prefix server"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
